test_that("multiplication works", {
  expect_equal(2 * 2, 4)
})

# act_push_summary(192, qualified = list.files(pattern = '_qualified.*csv', full.names = T), unqualified = list.files(pattern = '_unqualified.*csv',full.names = T), deployment = 'c:/users/darpa2/analysis/kennebec-sturgeon-monitoring/act-matos/master_metadata_deployment.xlsx')
